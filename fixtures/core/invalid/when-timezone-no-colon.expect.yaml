# Expected rejection for when-timezone-no-colon.yaml (core spec 8, Level 1).
# Registry: spec/registries/error-codes.yaml
# Schema:   schemas/hushspec-error-codes.v1.schema.json#/$defs/ExpectedError
reject: true
code: "E004"
message_contains: "is neither an IANA time zone nor"
