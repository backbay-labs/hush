# Expected rejection for when-too-deep.yaml (core spec 8, Level 1).
# Registry: spec/registries/error-codes.yaml
# Schema:   schemas/hushspec-error-codes.v0.schema.json#/$defs/ExpectedError
reject: true
code: "E004"
message_contains: "conditions nest deeper than the maximum"
