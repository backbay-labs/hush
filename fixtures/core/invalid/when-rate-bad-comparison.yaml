# D19 (core 3.13): comparison is gte or lt.
hushspec: "0.1.0"
rules:
  shell_commands:
    when:
      rate:
        counter: shell_commands
        threshold: 3
        comparison: between
    forbidden_patterns: ["rm[ \\t]+-rf"]
