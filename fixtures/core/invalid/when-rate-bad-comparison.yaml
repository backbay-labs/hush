# A rate condition's comparison is gte or lt (core spec 3.13).
hushspec: "0.1.0"
rules:
  shell_commands:
    when:
      rate:
        counter: shell_commands
        threshold: 3
        comparison: between
    forbidden_patterns: ["rm[ \\t]+-rf"]
