# Expected rejection for yaml-merge-key.yaml (core spec 8, Level 1).
# Registry: spec/registries/error-codes.yaml
# Schema:   schemas/hushspec-error-codes.v1.schema.json#/$defs/ExpectedError
reject: true
code: "E001"
message_contains: "anchors are not allowed"
