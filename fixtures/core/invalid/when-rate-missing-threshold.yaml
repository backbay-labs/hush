# A rate condition requires counter, threshold, and comparison (core spec 3.13).
hushspec: "0.1.0"
rules:
  shell_commands:
    when:
      rate:
        counter: shell_commands
        comparison: gte
    forbidden_patterns: ["rm[ \\t]+-rf"]
