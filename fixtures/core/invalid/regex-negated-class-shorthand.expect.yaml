# Expected rejection for regex-negated-class-shorthand.yaml (core spec 8, Level 1).
# Registry: spec/registries/error-codes.yaml
# Schema:   schemas/hushspec-error-codes.v1.schema.json#/$defs/ExpectedError
reject: true
code: "E005"
message_contains: "negated shorthand cannot be expressed as a class member"
