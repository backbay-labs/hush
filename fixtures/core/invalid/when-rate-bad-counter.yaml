# A counter name must match the identifier grammar (core spec 3.13).
hushspec: "0.1.0"
rules:
  shell_commands:
    when:
      rate:
        counter: "9lives"
        threshold: 3
        comparison: gte
    forbidden_patterns: ["rm[ \\t]+-rf"]
