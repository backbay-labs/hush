# Expected rejection for when-unknown-key.yaml (core spec 8, Level 1).
# Registry: spec/registries/error-codes.yaml
# Schema:   schemas/hushspec-error-codes.v0.schema.json#/$defs/ExpectedError
reject: true
code: "E001"
message_contains: "unknown field `weekday_only`"
