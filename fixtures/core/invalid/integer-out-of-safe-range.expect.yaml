# Expected rejection for integer-out-of-safe-range.yaml (canonical spec 4.3; core spec 8, Level 1).
# Registry: spec/registries/error-codes.yaml
# Schema:   schemas/hushspec-error-codes.v1.schema.json#/$defs/ExpectedError
reject: true
code: "E001"
message_contains: "exceeds the safe range"
