hushspec: 0.2.0
name: signed-basic
metadata:
  policy_version: 4
  author: security@example.com
rules:
  egress:
    allow:
    - api.example.com
    - evil.example.net
  shell_commands:
    forbidden_patterns:
    - mkfs
