# Expected rejection for bad-similarity.yaml (core spec 8, Level 1).
# Registry: spec/registries/error-codes.yaml
# Schema:   schemas/hushspec-error-codes.v1.schema.json#/$defs/ExpectedError
reject: true
code: "E004"
message_contains: "similarity_threshold must be between 0.0 and 1.0"
